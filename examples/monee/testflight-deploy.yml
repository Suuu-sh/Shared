name: TestFlight Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'Mobile/**'
      - '.github/workflows/testflight-deploy.yml'
  workflow_dispatch:

concurrency:
  group: testflight-deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and upload to TestFlight
    runs-on: macos-15
    timeout-minutes: 90
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Build, upload, and distribute internal build
        uses: <OWNER>/<SHARED_REPO>/actions/testflight-deploy@main
        with:
          project-path: Mobile/Monee.xcodeproj
          scheme: Monee
          configuration: Release
          app-bundle-id: com.yota.Monee
          team-id: 83J5SB3ZPX
          app-store-connect-key-id: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          app-store-connect-issuer-id: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          app-store-connect-private-key: ${{ secrets.APP_STORE_CONNECT_PRIVATE_KEY }}
          ios-distribution-certificate-p12-base64: ${{ secrets.IOS_DISTRIBUTION_CERTIFICATE_P12_BASE64 }}
          ios-distribution-certificate-password: ${{ secrets.IOS_DISTRIBUTION_CERTIFICATE_PASSWORD }}
          ios-app-store-provisioning-profile-base64: ${{ secrets.IOS_APP_STORE_PROVISIONING_PROFILE_BASE64 }}
          testflight-internal-group-name: ${{ vars.TESTFLIGHT_INTERNAL_GROUP_NAME }}
          testflight-internal-tester-emails: ${{ secrets.TESTFLIGHT_INTERNAL_TESTER_EMAILS }}
          testflight-notify-assigned-testers: 'true'
